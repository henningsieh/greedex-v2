/**
 * Participants Feature
 *
 * This module provides types and schemas for managing project participants
 * (users linked to mobility event projects).
 *
 * Note: Most participant-related operations are currently handled through
 * project procedures. This module provides the base types and schemas
 * for future expansion.
 */

export * from "./types";
export * from "./validation-schemas";
